git describe --tags --abbrev=0

git fetch origin
git reset --hard origin/main


git add .
git commit -m "Removed the idle detector, fps debug counter mechanic, added functionality to force game to launch on startup"

git push origin main


git tag v2.0.3
git push origin v2.0.3



